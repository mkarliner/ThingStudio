{
  // Server authentication info
  "servers": [
    {
      "host": "104.236.200.201",
      "username": "mike",
      //"password": ""
      // or pem file (ssh based authentication)
      "pem": "~/.ssh/id_rsa",
      "sshOptions": {"port": 4823}
    }
  ],

  // Install MongoDB in the server, does not destroy local MongoDB on future setup
  "setupMongo": false,

  // WARNING: Node.js is required! Only skip if you already have Node.js installed on server.
  "setupNode": false,

  // WARNING: If nodeVersion omitted will setup 0.10.36 by default. Do not use v, only version number.
  "nodeVersion": "0.10.36",

  // Install PhantomJS in the server
  "setupPhantom": true,

  // Show a progress bar during the upload of the bundle to the server. 
  // Might cause an error in some rare cases if set to true, for instance in Shippable CI
  "enableUploadProgressBar": false,

  // Application name (No spaces)
  "appName": "ThingStudio",

  // Location of app (local directory)
  "app": "/Users/mike/Src/ThingStudio/AppTest/AppsUI/ThingStudio",

  // Configure environment
  "env": {
    "HTTP_FORWARDED_COUNT": "1",
    "ROOT_URL": "http://www.thingstud.io",
    "PORT": 3000,
    "MONGO_URL": "mongodb://thingstudio:electr1c@c196.capital.3.mongolayer.com:10196,capital.2.mongolayer.com:10196/ThingStudioProduction?replicaSet=set-5551f34f1e0a97cce4000c18"
  },

  // Meteor Up checks if the app comes online just after the deployment
  // before mup checks that, it will wait for no. of seconds configured below
  "deployCheckWaitTime": 60
}
